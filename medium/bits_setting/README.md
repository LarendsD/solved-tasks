# Bits Setting

Create a function that sets bits of bitmask to bitstring. Use logical operator OR. Size of bitstring and bitmask are the same.

Examples:
```js
"101101"  == solution("101101", "101101")
"1111"  == solution("1011", "0111")
"111"  == solution("101", "110")
```